# 生产环境（.env）：MySQL 与应用在同一台服务器，host 用 127.0.0.1
DATABASE_URL=mysql+pymysql://user:password@127.0.0.1:3306/dbname

# 本地开发（.env.dev）：连接远程 MySQL
# DATABASE_URL=mysql+pymysql://user:password@remote-host:3306/dbname
